name: github action demo
on: push
jobs:
    build:
      name: Build
      runs-on: ubuntu-24.04
      steps:
        - name: Checkout code
          uses: actions/checkout@v7
        - name: Build Project
          run: mvn clean install

